const layout = () => {
    const $document = $(document);
    const $window = $(window);

    const checkWindowSize = () => {
        // console.log($window.width())
    }

    const init = () => {
        checkWindowSize();
    }

    init();
}

export default layout;